<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.assistapp_sum">

    <!-- 권한 -->
    <uses-permission android:name="android.permission.CAMERA"
        tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.Assistapp"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 기능 액티비티들 -->
        <activity android:name=".features.feature_manager.FeatureManagerActivity" />
        <activity android:name=".features.fun1_schedulecheck.ScheduleCheckActivity" />
        <activity android:name=".features.fun2_navigation.NavigationActivity" />
        <activity android:name=".features.fun3_barcode.BarcodeRecognitionActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>
        <activity android:name=".features.fun4_hand.LevelSelectActivity" />
        <activity android:name=".features.fun4_hand.HandTrainingActivity" />
        <activity android:name=".features.fun5_bill.BillRecognitionActivity" />
    </application>
</manifest>
